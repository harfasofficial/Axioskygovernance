services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # REQUIRED: Set all of these in a .env file or your deployment secrets manager.
      # Do NOT use the defaults -- they are insecure placeholders.
      - DATABASE_URL=postgresql://axiosky:${DB_PASSWORD}@db:5432/axiosky
      - REDIS_URL=redis://redis:6379
      - SECRET_KEY=${SECRET_KEY}
      - ENVIRONMENT=${ENVIRONMENT:-development}
      - AXIOSKY_PUBLIC_BASE_URL=${AXIOSKY_PUBLIC_BASE_URL:-http://localhost:8000}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000}
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - RATE_LIMIT_PER_MINUTE=${RATE_LIMIT_PER_MINUTE:-100}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          memory: 256M

  migrate:
    # Separate migration service -- run once before starting api.
    # Usage: docker-compose run --rm migrate
    build: .
    command: ["python", "-m", "database.migrate"]
    environment:
      - DATABASE_URL=postgresql://axiosky:${DB_PASSWORD}@db:5432/axiosky
      - ENVIRONMENT=${ENVIRONMENT:-development}
    depends_on:
      db:
        condition: service_healthy
    profiles:
      - migrate

  db:
    image: postgres:16  # Upgraded from 14 (EOL Nov 2026)
    environment:
      POSTGRES_USER: axiosky
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: axiosky
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U axiosky -d axiosky"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1G

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M

volumes:
  postgres_data:
  redis_data:
